import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Rectify - Energy Recruitment & Consultancy',
  description: 'Sourcing the talent of tomorrow\'s future. Experts in energy recruitment and consultancy, helping businesses find business-critical talent to build a sustainable future.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
